/**
 * footballVoice.ts — Football culture voice spec (STUB).
 *
 * Voice TBD. When ready: define a register that fits football's
 * positional structure (QB-driven narratives, defensive star arcs,
 * coaching tree references, rivalry weeks).
 *
 * Until then, throwing on import signals "voice unset — do not run a
 * culture generation in football mode."
 */

export const FOOTBALL_VOICE = `STUB — football voice spec not yet defined.
Once defined, the structure should mirror basketballVoice.ts:
  - Voice register (analogous to "Norman Chad at a sportsbook")
  - Gold-standard examples (5-10)
  - Anti-examples (3-5)
  - Team-era specificity rules
  - Field structural rules
  - Trademark + factual + personal-life rules

Do not run a culture generation in football mode against this stub —
the model will treat it as a literal instruction and emit malformed output.`;
