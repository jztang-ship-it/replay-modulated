/**
 * shared/components/types.ts
 * Component-level types shared across all sports' UI layers.
 * These are distinct from engine/data types in shared/types/index.ts.
 */

/** Shake animation style for card reveal sequence */
export type ShakeType = "big" | "hype" | "cold" | null;