/**
 * shared/views/GameView.tsx
 *
 * Phase 2 stub. Full lift lands in tasks 3-6. Each sport's wrapper
 * passes a GameAdapter; this stub doesn't use it yet, just satisfies
 * the prop contract.
 */

import type { GameAdapter } from "./GameAdapter";

interface Props {
  adapter: GameAdapter;
}

export function GameView(_props: Props): null {
  return null;
}
